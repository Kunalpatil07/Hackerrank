#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a, b;
    int *pa = &a, *pb = &b;
    
    scanf("%d %d", &a, &b);
    update(pa, pb);
    int c = abs(a+ b);
    int d = abs(a-b);
    printf("%d \n", c);
    printf("%d", d);
    return 0;
}
